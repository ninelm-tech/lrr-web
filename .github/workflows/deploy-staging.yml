name: Deploy → Staging

on:
  push:
    branches:
      - staging
    paths:
      - '**'
      - '!.github/workflows/**'
      - '.github/workflows/deploy-staging.yml'

env:
  AWS_REGION: us-east-2
  ECR_REPOSITORY: lrr/web
  ECS_CLUSTER: lrr-staging
  ECS_SERVICE: lrr-web-staging
  IMAGE_TAG: staging

jobs:
  deploy:
    name: Build & Deploy to Staging
    runs-on: ubuntu-latest

    # Required for OIDC token exchange with AWS
    permissions:
      id-token: write
      contents: read

    steps:
      # ── Checkout ──────────────────────────────────────────────────
      - name: Checkout code
        uses: actions/checkout@v4

      # ── AWS credentials (OIDC — no long-lived keys needed) ────────
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::808236751302:role/github-actions-lrr
          aws-region: ${{ env.AWS_REGION }}

      # ── Login to ECR ──────────────────────────────────────────────
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # ── Build & push image ────────────────────────────────────────
      # NEXT_PUBLIC_* values are baked into the JS bundle at build time, so
      # this build must use staging's own values — never copy the prod job's.
      - name: Build, tag, and push image to ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          IMAGE_URI=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "Building $IMAGE_URI"
          docker build \
            --cache-from $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            --build-arg NEXT_PUBLIC_API_URL="${{ vars.STAGING_NEXT_PUBLIC_API_URL }}" \
            --build-arg NEXT_PUBLIC_WHATSAPP_NUMBER="${{ vars.STAGING_NEXT_PUBLIC_WHATSAPP_NUMBER }}" \
            --build-arg NEXT_PUBLIC_WHATSAPP_DISPLAY="${{ vars.STAGING_NEXT_PUBLIC_WHATSAPP_DISPLAY }}" \
            --build-arg NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="${{ secrets.STAGING_NEXT_PUBLIC_GOOGLE_MAPS_API_KEY }}" \
            --build-arg NEXT_PUBLIC_SENTRY_DSN="${{ secrets.STAGING_NEXT_PUBLIC_SENTRY_DSN }}" \
            --build-arg SENTRY_AUTH_TOKEN="${{ secrets.SENTRY_AUTH_TOKEN }}" \
            -t $IMAGE_URI \
            .
          docker push $IMAGE_URI
          echo "image=$IMAGE_URI" >> $GITHUB_OUTPUT

      # ── Force new ECS deployment ──────────────────────────────────
      - name: Deploy to ECS
        run: |
          aws ecs update-service \
            --cluster $ECS_CLUSTER \
            --service $ECS_SERVICE \
            --force-new-deployment \
            --region $AWS_REGION

      # ── Wait for service to stabilise ─────────────────────────────
      - name: Wait for deployment to complete
        run: |
          echo "Waiting for ECS service to stabilise..."
          aws ecs wait services-stable \
            --cluster $ECS_CLUSTER \
            --services $ECS_SERVICE \
            --region $AWS_REGION
          echo "✅ Deployment complete"
